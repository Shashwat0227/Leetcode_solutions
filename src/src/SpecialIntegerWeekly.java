import java.util.*;
public class SpecialIntegerWeekly {
    public int minimumOperations(String num) {
        int count =0;
        int n = num.length();
        int []arr = new int [n];
        for(int i =0 ; i<arr.length ;i++){
            arr[i]=num.charAt(i);
        }
        if(num.charAt(0)!='0'|| num.charAt(0)!='5'){
        }
    }
}
